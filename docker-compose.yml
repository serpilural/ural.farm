services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # backend:
  #   build: ./backend
  #   ports:
  #     - "8000:8000"
  #   restart: unless-stopped
